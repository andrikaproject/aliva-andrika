services:
  web:
    networks:
      private: {}
      edge:
        aliases: [aliva-web]
  api:
    networks:
      private: {}
      edge:
        aliases: [aliva-api]

networks:
  edge:
    external: true
    name: photobooth_default
